export const connectESP = () => {
  console.log("Connecting to ESP...");
};

export const sendMessage = (message) => {
  console.log("Sending message:", message);
};

export const disconnectESP = () => {
  console.log("Disconnecting ESP...");
};
